export class Book {
    constructor(title, author, pages, isReaded, id) {
        this.title = title;
        this.author = author;
        this.pages = pages;
        this.isRead = isReaded;
        this.id = id;
    }
}
